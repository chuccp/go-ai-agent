# Go AI Agent

> 🚧 **開発中** — このプロジェクトは活発に開発中です。Claude Fable 5 を使って機能の改善・完成にご協力いただける方を歓迎します。ありがとうございます！

クロスプラットフォームデスクトップ AI エージェントプラットフォーム。**チャットで AI ワークフローを作成** — 自然言語でやりたいことを説明するだけで、エージェントがパイプラインを設計・構築・実行します。

**Wails v2** + **React** + **Go** で構築。

[English](README.md) | [简体中文](README.zh-CN.md) | [繁體中文](README.zh-TW.md)

## チャットによるワークフロー作成の利点

従来のワークフローツールでは、ビジュアルエディタの使い方を学ぶ必要がありました — ノードをドラッグし、エッジを配線し、パラメータを設定します。Go AI Agent では、必要なことを伝えるだけです：

> *「最新のAIニュースを取得し、DeepSeekで要約し、要約を日本語に翻訳して、保存前に私にレビューさせてください」*

エージェントは**意図を理解 → ノード構造を提案 → 確認を待つ → 自動的にフローを作成**します。手動配線も設定の推測も不要です。会話を通じて反復的に改善できます — 「モデルをGPT-5に変更して」「要約の前に感情分析ステップを追加して」 — エージェントがすぐにフローを更新します。

**手動エディタと比較した利点：**

- **学習不要** — ノードタイプ、接続ルール、設定スキーマを学ぶ必要なし
- **自然な反復** — 同僚と話すように、会話を通じてフローを洗練
- **設計ガイダンス** — エージェントがベストプラクティスを提案（例：「このフローには送信前のユーザー確認ステップが必要です」）
- **高速プロトタイピング** — アイデアから動作するパイプラインまで1分未満
- **完全な制御** — ビジュアルエディタでいつでも手動微調整が可能

## 機能

- **チャットによるワークフロー作成** — 自然言語での会話を通じて `manage_flows` ツールで AI パイプラインを構築
- **ワンクリック共有** — フローをJSONファイルとしてエクスポート、チームメンバーがワンクリックでインポート、実戦で磨かれたAIワークフローを共有ライブラリとして蓄積
- **ビジュアルフローデザイナー** — Difyスタイルのドラッグ＆ドロップDAGエディタ、14種類のノード、必要に応じて手動編集可能
- **デスクトップアプリ** — Wails v2によるネイティブ macOS/Windows/Linux ウィンドウ、ダブルクリックで起動
- **ワンステップ設定** — デスクトップ版はSQLite + 管理者アカウントを自動設定、モデルAPIキーのみ必要
- **マルチモデル** — OpenAI、Claude、Gemini、DeepSeekなど28以上のプロバイダーに統一インターフェースで対応
- **Agentツール実行** — 組み込みツール実行ループ、拡張可能なツールレジストリ
- **ストリーミングチャット** — WebSocketベースのリアルタイムストリーミング、思考プロセス表示付き
- **Webモード** — `--web` フラグでブラウザサーバーとして起動（SQLite/MySQL/PostgreSQL対応）
- **多言語** — English, 简体中文, 繁體中文, 日本語

## クイックスタート

### デスクトップ版

```bash
# 前提条件: Go 1.25+, Node 18+, pnpm
go install github.com/wailsapp/wails/v2/cmd/wails@latest

git clone https://github.com/chuccp/go-ai-agent.git
cd go-ai-agent

# 開発モード（ホットリロード）
wails dev

# macOS .app をビルド
wails build
```

ビルド成果物は `build/bin/go-ai-agent.app`。ダブルクリックで起動、初回起動時はモデルAPIキーの設定のみ必要です。

### Webサーバーモード

```bash
go build -o go-ai-agent . && ./go-ai-agent --web
cd view && pnpm dev
```

`http://localhost:5173` を開くと、初回実行時にセットアップウィザードが表示されます。

## 仕組み

```
あなた: 「コンテンツレビューフローを作成して」
         │
         ▼
┌─────────────────────────────────┐
│  AI エージェント                  │
│  (manage_flows ツール)           │
│                                 │
│  1. 理解 — フローについての     │
│     詳細な質問を投げかける      │
│  2. 設計 — 自然言語で           │
│     ノード構造を提案            │
│  3. 確認 — ユーザーの明示的な   │
│     承認を待つ                  │
│  4. 作成 — 正しいノードと       │
│     エッジでフローを自動構築    │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  自動生成されたビジュアルフロー   │
│  ┌───┐   ┌──────┐   ┌───┐     │
│  │開始│──▶│ LLM  │──▶│終了│     │
│  └───┘   └──────┘   └───┘     │
│                                 │
│  ドラッグ＆ドロップエディタで    │
│  いつでも手動編集可能            │
└─────────────────────────────────┘
```

## 会話の中でフローを実行

フローは単独で動くスクリプトではありません——チャット会話の中で直接呼び出せます。エージェントがランタイムとなり、フローを読み込み、ステップごとに実行し、会話の中で進捗を報告します。

```
あなた: 「ニュースレビューフローでこの記事を分析して：[貼り付け]」
         │
         ▼
┌───────────────────────────────────────┐
│  エージェントが会話しながら           │
│  フローをステップ実行                 │
│                                       │
│  エージェント: "Step 1/4 - 要約中..." │
│  エージェント: "Step 2/4 - 感情: ネガ  │
│  ティブ"                              │
│  ┌──────────────────────────┐        │
│  │  user_input ノード発動    │        │
│  │  "内容がネガティブです。   │        │
│  │   それでも公開しますか？    │        │
│  │   それともスキップ？"      │        │
│  └──────────────────────────┘        │
│                                       │
│  あなた: 「スキップして」             │
│  エージェント: 「スキップしました。   │
│                フロー完了」           │
└───────────────────────────────────────┘
```

フローが**構造化された作業**を処理し、会話が**意思決定**を担当します。フローが実行を推進し、判断が必要な場面では常にあなたがループの中にいます。既存のフローをチャット中に実行することも、その場で新しいフローを作成することも——チャットと実行の境界はシームレスです。

## フロー vs スキル

AI スキル（単一のプロンプトやツール呼び出し）は単発技、フローは連続技の完全なプレイブックです。

| | スキル | フロー |
|---|------|-------|
| **構造** | 単発 — プロンプト + 単一アクション | 多段階 DAG — 14種類のノードを自由に組み合わせ |
| **分岐** | なし、リニアのみ | 条件分岐、テキスト分割、並列バッチ処理 |
| **ヒューマンインザループ** | 途中停止不可 | `user_input` ノードでレビュー・承認・追加入力を待機 |
| **状態受け渡し** | ステートレス | `{{ノード名.output}}` テンプレートでノード間データ連携 |
| **可観測性** | 最終結果のみ | 全ノードの入力・出力・ステータスを追跡・記録 |
| **マルチモデル** | 単一モデル | ノードごとに異なるモデルを使用可能（GPT翻訳 → Claude推敲 → Geminiレビュー） |
| **並行処理** | なし | `for_each` 並列、`iterator` 逐次、`loop` 条件付き反復 |

**具体例** — *「ニュース監視 → DeepSeek要約 → 感情分析 → ネガティブは人間レビュー → ポジティブは直接公開」*

- **スキルの場合**：5つのプロンプトを手動実行、自分で結果をチェック、ポジ/ネガを手動振り分け
- **フローの場合**：1本のパイプラインが自動実行 — conditionノードが自動分岐、ネガティブは確認待ち、ポジティブはそのまま公開

フローは人間の判断力とAIの能力を一つのワークフローに編成し、AIを孤立した単発ツールとしてではなく、プロセスの一部として活用します。

### フローがより安定している理由

単一スキルは「神関数」のようなものです——1回のLLM呼び出しで理解・推論・判断・出力をすべて行わなければなりません。フローはそれを決定論的な部品に分解します：

- **単一責任** — 各ノードは1つのことだけをします。LLMノードは生成、conditionノードは振り分け、transformノードはデータ整形。混乱の元がありません。
- **明示的な状態受け渡し** — `{{ノード.output}}` テンプレートでノード間のデータ連携。LLMが「覚える」必要なし——下流ノードは毎回正確な入力を受け取ります。
- **分岐の外部化** — conditionノードはシンプルなcontains/equalsルールで判断。LLMがデータの行き先を決めるのではなく、DAGが決めます。
- **人間チェックポイント** — `user_input` ノードが実行を一時停止。人間が中間結果を確認し、フローが続行する前に方向を修正できます。
- **コンテキスト長の削減、注意力の向上** — スキルはすべてを1つの巨大なプロンプトに詰め込み、モデルの注意力を分散させます。フローは各ノードに焦点を絞った短いコンテキストを渡し、LLMは一度に1つのことだけ考えればよい——コンテキストが短いほど、注意力が高まり、出力の信頼性が向上します。

大きな信頼性の低いプロンプトを、小さな信頼性の高いステップ + 決定論的オーケストレーション + 人間の安全策に分解します。

## 共有と再利用

フローはJSONファイルとして保存され、ワンクリックでエクスポート、ファイルを共有、ワンクリックでインポートして実行できます。

- **チームライブラリ** — 実戦で検証されたワークフローをチーム共有資産として蓄積、新人もインポートするだけですぐに使えます
- **コミュニティテンプレート** — GitHubやDiscordなどでフローを公開共有、誰でもインポートして実行可能
- **ロックインなし** — JSONフォーマットは可読性が高く、単一プラットフォームに依存しません
- **即時オンボーディング** — 新しいメンバーは共有フローをインポートすれば即座に生産的になれます

```
作成 → JSONエクスポート → 共有 → インポート → 実行
   │                                        │
   └──── 反復改善 & 再共有 ←────────────────┘
```

## 技術スタック

| レイヤー | 技術 |
|---------|------|
| デスクトップシェル | Wails v2 (システム WebView) |
| バックエンド | Go + go-web-frame + Gorilla WebSocket |
| フロントエンド | React 18 + TypeScript + Vite |
| フローエディタ | reactflow + Zustand |
| チャット UI | @assistant-ui/react |
| 国際化 | react-i18next |
| データベース | SQLite (デスクトップ) / MySQL / PostgreSQL (Web) |

## プロジェクト構成

```
go-ai-agent/
├── main.go              # エントリポイント、--web フラグ、デスクトップ/Web ルーティング
├── app.go               # Wails App 構造体、アセット埋め込み
├── desktop_init.go      # デスクトップ版自動初期化（SQLite + 管理者）
├── wails.json           # Wails プロジェクト設定
├── Makefile             # ビルドターゲット
├── agent/               # エージェントループ、ツールレジストリ
├── ai/chat/             # 統合チャットサービス + 28以上のプロバイダー
├── runner/              # ChatRunner, FlowRunner
├── rest/                # REST エンドポイント
├── flow/                # フローエンジン (DAG エグゼキューター, 14種類のノード)
└── view/                # React フロントエンド
    └── src/
        ├── pages/       # チャットホーム, フローデザイナー, モデル管理, セットアップウィザード
        ├── components/  # チャット UI, フローエディタ, 共通コンポーネント
        ├── stores/      # Zustand 状態管理
        └── i18n/        # 多言語ファイル
```

## License

MIT
